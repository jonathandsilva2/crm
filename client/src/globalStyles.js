import { createGlobalStyle } from 'styled-components';

// Global styles for the app
export const GlobalStyle = createGlobalStyle`
body {
  margin: 0;
  padding: 0;
}
`;

// Day and night themes for
export const dayTheme = {
  primary: 'black',
};

export const nightTheme = {};
